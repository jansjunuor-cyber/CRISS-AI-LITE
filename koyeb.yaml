name: CRISS-AI-LITE
services:
  - name: CRISS-AI-LITE-service
    type: web
    ports:
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin
      
